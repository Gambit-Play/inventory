const theme = {
	// Functions
	radius: number => `${number * 4}px`,
	spacing: number => `${number * 4}px`,
	fontSize: number => `${number * 4}px`,

	// Colors
	primary: '#3f51b5',
	secondary: '#f50057',
};

export default theme;
